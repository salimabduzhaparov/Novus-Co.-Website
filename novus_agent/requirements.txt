# Core (required)
anthropic>=0.79.0
sqlalchemy>=2.0
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4
httpx>=0.27
beautifulsoup4>=4.12
apscheduler>=3.10

# Gmail drafts/sending/reply-polling (required for real outreach; agent degrades to ./outbox EML files without it)
google-api-python-client>=2.140
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# Higgsfield MCP (bespoke imagery + website deploy). Optional but recommended.
mcp>=1.1

# Rendered-browser self-checks (console errors, mobile overflow). Optional; selfcheck degrades gracefully.
playwright>=1.46
